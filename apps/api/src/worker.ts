// Entrypoint for the worker process: polls the jobs table and runs scans
// sequentially (concurrency 1). See docs/CONCEPT.md 0.1/0.2 and section 5.
//
// Not implemented yet — the poll loop, job locking, and scan pipeline are
// built in implementation steps 1–2 (docs/CONCEPT.md section 10).

console.log('LazySentry worker: not implemented yet');
process.exit(0);
