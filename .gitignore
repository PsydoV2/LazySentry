# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Environment — never commit real secrets (see .env.example)
.env
.env.local

# Local database & runtime data
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Test & tooling
coverage/

# Local scanner binaries for development (baked into the Docker image in prod)
tools/

# Playwright MCP artifacts
.playwright-mcp/

# OS / editor noise
.DS_Store
Thumbs.db
